<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- RF-03 | Permiso de hardware: cámara (peligroso, se solicita en tiempo de ejecución) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- RF-03 | Permiso de hardware: vibración (normal, se otorga al instalar) -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- RF-07 | Necesario para el streaming de audio con Media3 ExoPlayer -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.IUDigitalRadio">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.IUDigitalRadio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
